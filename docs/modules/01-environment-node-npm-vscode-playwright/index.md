# Модуль 01: Окружение, Node.js, npm, VS Code и Playwright

## Главное в модуле

- роль Node.js, npm и VS Code в рабочем окружении;
- демонстрация создания нового проекта Playwright;
- практическая работа в подготовленном репозитории домашних заданий;
- запуск тестов и чтение HTML report.

## Демонстрация и практический сценарий курса

В этом модуле есть две разные части.

Первая часть — демонстрация. В ней показано, как в целом создаётся новый проект Playwright, какие вопросы задаёт установщик и какие файлы появляются после установки.

Вторая часть — практический сценарий курса. Домашние задания выполняются в подготовленном репозитории:

`https://github.com/EugeneChirkun/Pw-Ts-Qa-Hw`

Для практики студент клонирует этот репозиторий, открывает его в VS Code, переходит в свою ветку модуля и выполняет задание внутри подготовленной структуры.

## Минимальный результат

После модуля студент должен уметь:

- проверить версии Node.js и npm;
- установить зависимости подготовленного проекта;
- запустить тесты и открыть отчёт.


## Цель модуля

После этого модуля вы сможете подготовить базовое рабочее окружение Automation QA: установить и проверить Node.js и npm, понять базовый процесс создания проекта Playwright, склонировать подготовленный репозиторий домашних заданий, установить зависимости, запустить подготовленные тесты, открыть HTML report и разобраться с назначением основных файлов проекта Playwright.

Также вы поймете, зачем Automation QA нужны инструменты вокруг тестов: зависимости, структура проекта, `package.json`, `node_modules`, терминал, VS Code и test runner.

## Почему это важно для Automation QA

Automation QA — это не только написание тестов. В реальной работе нужно уверенно пользоваться инструментами, понимать структуру проекта, устанавливать зависимости, запускать команды, читать ошибки, настраивать скрипты и анализировать отчёты.

Если окружение настроено неправильно, тесты могут не запускаться даже при корректном коде. Поэтому первый шаг в автоматизации — понять, из каких частей состоит рабочая среда и как они связаны между собой.

## Темы модуля

- Что входит в рабочее окружение Automation QA.
- Что такое Node.js и зачем он нужен для Playwright.
- Что такое npm, зависимости и скрипты.
- Демонстрация: как создаётся новый проект Playwright.
- Практический сценарий курса: работа в подготовленном репозитории домашних заданий.
- Как клонировать репозиторий домашних заданий.
- Как перейти в свою ветку модуля.
- Как установить зависимости через `npm ci` или `npm install`.
- Как установить браузеры Playwright через `npx playwright install`.
- Как запустить подготовленный smoke-тест.
- Как открыть HTML report.
- Как прочитать первый простой тест Playwright.

## 1. Что такое рабочее окружение Automation QA

Рабочее окружение Automation QA — это набор инструментов, которые нужны, чтобы писать, запускать и поддерживать автоматизированные тесты.

Обычно окружение включает:

- Node.js для запуска JavaScript и инструментов TypeScript вне браузера.
- npm для установки пакетов и запуска скриптов.
- VS Code как редактор кода.
- Playwright как фреймворк и test runner.
- Браузеры, которые Playwright использует для запуска тестов.
- Git и GitHub для хранения кода и совместной работы.
- Терминал для выполнения команд.

Для Manual QA это похоже на набор инструментов для тестирования: браузер, DevTools, система управления тестами, баг-трекер и тестовая документация. В Automation QA добавляются редактор кода, зависимости, командная строка и отчёты.

## 2. Node.js

Node.js — это среда выполнения JavaScript вне браузера. Обычно JavaScript выполняется внутри браузера, например на странице сайта. Node.js позволяет запускать JavaScript и инструменты на вашем компьютере через терминал.

TypeScript сам по себе тоже не запускается напрямую в браузере или терминале как обычный готовый код. В проекте используются инструменты, которые компилируют или обрабатывают TypeScript. Эти инструменты работают через Node.js.

Playwright для TypeScript использует Node.js, потому что:

- test runner Playwright запускается в Node.js.
- npm-пакеты устанавливаются и управляются через экосистему Node.js.
- команды Playwright выполняются из терминала.
- тесты, конфигурация и отчёты обрабатываются инструментами Node.js.

Установите Node.js с [официальной страницы загрузки](https://nodejs.org/en/download). Для обучения и реальной работы выбирайте LTS-версию. LTS означает Long Term Support: это рекомендованный стабильный вариант с длительной поддержкой.

После установки проверьте версии Node.js и npm командами:

```bash
node -v
npm -v
```

Если обе команды показывают номера версий, значит Node.js и npm доступны в терминале.

## 3. npm

npm — это менеджер пакетов для Node.js. Он помогает устанавливать пакеты, управлять зависимостями и запускать скрипты из `package.json`.

Пакет — это готовая библиотека или инструмент. Например, Playwright устанавливается как npm-пакет.

Зависимость — это пакет, который нужен проекту для работы, разработки или тестирования. Зависимости бывают двух основных типов:

- `dependencies` — зависимости, которые нужны приложению во время работы.
- `devDependencies` — зависимости, которые нужны для разработки, тестирования, сборки или проверки кода.

В проекте автоматизации Playwright обычно находится в `devDependencies`, потому что он нужен для разработки и запуска тестов, а не для работы пользовательского приложения.

### package.json

`package.json` — главный файл проекта Node.js. В нем описаны название проекта, скрипты, `dependencies` и `devDependencies`.

Пример поля `scripts` в `package.json`:

```json
{
  "scripts": {
    "test": "playwright test"
  }
}
```

После этого команду можно запускать так:

```bash
npm run test
```

### package-lock.json

`package-lock.json` фиксирует точные версии установленных пакетов и их внутренних зависимостей. Это помогает всей команде устанавливать одинаковые версии и получать более предсказуемый результат.

Обычно `package-lock.json` нужно добавлять в commit в репозитории.

### node_modules

`node_modules` — это папка, куда npm устанавливает пакеты. Она может быть очень большой, потому что содержит все зависимости проекта.

`node_modules` не нужно добавлять в commit в Git. Вместо этого в репозитории хранят `package.json` и `package-lock.json`, а каждый разработчик или CI-сервер восстанавливает зависимости командой `npm install` или `npm ci`.

### npm install

`npm install` устанавливает зависимости из `package.json` и обновляет `package-lock.json`, если это необходимо.

```bash
npm install
```

Эта команда часто используется локально во время разработки.

### npm ci

`npm ci` устанавливает зависимости строго по `package-lock.json`. Команда обычно используется в CI, потому что дает более чистую и воспроизводимую установку.

```bash
npm ci
```

### npm run

`npm run` запускает скрипт из `package.json`.

```bash
npm run test
```

Это удобно, потому что команда становится короткой и одинаковой для всей команды.

### npx

`npx` запускает команду пакета без необходимости писать полный путь до исполняемого файла в `node_modules`.

Например:

```bash
npx playwright test
```

Эта команда запускает локально установленный Playwright test runner.

## 4. VS Code

VS Code — это основной редактор для учебного проекта. В нём вы будете писать TypeScript-код, создавать тесты Playwright, запускать команды и разбирать ошибки. Установите VS Code локально с [официальной страницы загрузки](https://code.visualstudio.com/download).

В этом курсе VS Code нужен не только как место для набора кода. Вы будете использовать:

- встроенный терминал для команд `node -v`, `npm -v`, `npm run test`, `npx playwright test` и других команд Playwright;
- дерево файлов, чтобы быстро находить `package.json`, `playwright.config.ts`, папку `tests/` и файлы тестов;
- поиск по проекту, чтобы находить файлы, тесты, локаторы и селекторы;
- расширения, которые упрощают написание, запуск и отладку тестов;
- панель Testing после установки расширения Playwright Test for VS Code;
- сообщения об ошибках в терминале, чтобы переходить к файлам и строкам, где возникла проблема.

Automation QA часто работает в цикле: изменить тест, запустить команду, прочитать ошибку, исправить код, снова запустить тест. VS Code помогает делать это в одном месте.

### Что нужно уметь в VS Code

Для начала достаточно уверенно выполнять базовые действия:

- открывать папку как проект;
- открывать и использовать встроенный терминал;
- создавать новый файл;
- создавать новую папку;
- искать по всему проекту;
- устанавливать расширения;
- запускать команды из терминала;
- читать сообщения об ошибках;
- пользоваться панелью Testing;
- запускать тест Playwright из редактора;
- открывать HTML report из терминала.

### Рекомендуемые расширения VS Code

- Playwright Test for VS Code — позволяет запускать и отлаживать тесты Playwright из редактора, а также видеть тесты в панели Testing.
- ESLint — помогает находить проблемы со стилем и качеством кода.
- Prettier — автоматически форматирует код.
- GitLens — помогает смотреть историю Git и изменения в файлах. В модуле 01 это расширение необязательное.
- DotENV — подсвечивает файлы `.env`. Оно станет полезнее позже, когда появятся переменные окружения.

Пока не нужно глубоко настраивать расширения. На этом этапе важно установить Playwright Test for VS Code и понять, где находится панель Testing.

### Как открыть терминал в VS Code

Откройте меню **Terminal → New Terminal**. Сочетание клавиш может отличаться в зависимости от операционной системы, поэтому меню — самый простой и надёжный способ для начала.

Терминал должен открыться внутри текущей папки проекта. В нём можно запускать команды:

```bash
node -v
npm -v
npx playwright test
```

Если команда не находит файлы проекта или Playwright, сначала проверьте, что в VS Code открыта правильная папка.

### Как понять, что проект открыт правильно

В дереве файлов должны быть видны файлы и папки учебного проекта, например:

- `package.json`;
- `playwright.config.ts`;
- `tests/`.

Если этих файлов не видно, возможно, открыта не папка проекта, а соседняя или родительская папка. В таком случае откройте именно папку учебного проекта Playwright.

## Демонстрация: установка Playwright и создание нового проекта

Команда ниже показывает, как обычно создаётся новый проект Playwright:

```bash
npm init playwright@latest
```

Установщик задаёт несколько вопросов о настройке проекта. Для стека этого курса выберите TypeScript, папку с тестами можно оставить `tests`, а установку браузеров — включить. Выбор GitHub Actions зависит от того, нужен ли в новом проекте пример настройки CI.

Эта демонстрация помогает понять, откуда в новом проекте появляются `package.json`, `package-lock.json`, `playwright.config.ts`, `tests/` и другие файлы. Официальное стартовое руководство приведено на странице «Ресурсы» этого модуля.

Создать проект Playwright также можно через расширение Playwright Test for VS Code. Независимо от способа создания Automation QA важно понимать команды терминала: они используются в CI, документации, онбординге и командной работе.

## Практический сценарий курса: работа в подготовленном репозитории

После демонстрации практическая работа выполняется в репозитории домашних заданий:

`https://github.com/EugeneChirkun/Pw-Ts-Qa-Hw`

В нём уже подготовлена базовая структура проекта:

- `package.json`;
- `package-lock.json`;
- `playwright.config.ts`;
- `tests/`;
- `src/`;
- `homework/`;
- пример smoke-теста;
- npm-скрипты для запуска тестов и проверки типов.

Клонируйте репозиторий:

```bash
git clone https://github.com/EugeneChirkun/Pw-Ts-Qa-Hw.git
cd Pw-Ts-Qa-Hw
git fetch origin --prune
```

Перейдите в свою ветку модуля:

```bash
git switch --track origin/student/{student-name-slug}/module-01-environment
```

Если локальная ветка уже создана:

```bash
git switch student/{student-name-slug}/module-01-environment
```

Установите зависимости и браузеры:

```bash
npm ci
npx playwright install
```

Проверьте проект:

```bash
npm run typecheck
npm run test:smoke
npm run test
npm run report
```

## 6. Структура проекта после установки

После установки проект Playwright обычно содержит такие файлы и папки:

```text
tests/
playwright.config.ts
package.json
package-lock.json
node_modules/
```

Что это значит:

- `tests/` — папка с файлами тестов.
- `playwright.config.ts` — файл конфигурации Playwright: браузеры, повторные запуски, отчёты, `baseURL` и другие настройки.
- `package.json` — скрипты и зависимости проекта.
- `package-lock.json` — зафиксированные точные версии зависимостей.
- `node_modules/` — установленные пакеты, которые не нужно добавлять в commit в Git.

## 7. Первый запуск тестов

Основные команды для первого запуска:

```bash
npx playwright test
npx playwright test --headed
npx playwright test --ui
npx playwright show-report
```

Что делает каждая команда:

- `npx playwright test` запускает все тесты в headless-режиме, то есть браузеры открываются без видимого окна.
- `npx playwright test --headed` запускает тесты с видимым окном браузера. Это полезно для обучения и отладки.
- `npx playwright test --ui` открывает режим Playwright UI, где можно выбирать тесты, смотреть шаги и быстрее разбираться в поведении.
- `npx playwright show-report` открывает HTML report после запуска тестов.

HTML report помогает увидеть, какие тесты прошли, какие упали, сколько времени занял запуск и где произошла ошибка.

## 8. Объяснение первого теста

Пример первого теста:

```ts
import { test, expect } from '@playwright/test';

test('Playwright homepage has correct title', async ({ page }) => {
  await page.goto('https://playwright.dev/');

  await expect(page).toHaveTitle(/Playwright/);
});
```

Разберем по строкам:

- `import { test, expect } from '@playwright/test';` подключает из Playwright две главные функции: `test` для описания тестового случая и `expect` для проверки результата.
- `test('Playwright homepage has correct title', ...)` создает тест с понятным названием.
- `async` показывает, что внутри теста будут асинхронные операции: действия в браузере, переходы по страницам и проверки.
- `({ page })` — это Playwright fixture. `page` представляет вкладку браузера, с которой взаимодействует тест.
- `await page.goto('https://playwright.dev/');` открывает сайт Playwright.
- `expect(page).toHaveTitle(/Playwright/)` проверяет, что заголовок страницы содержит слово `Playwright`.
- Такая проверка называется assertion: она сравнивает ожидаемый результат с фактическим.
- `await` говорит test runner подождать завершения действия или проверки перед переходом к следующей строке.

Пока не нужно глубоко разбирать `async` и `await`. В этом модуле важно понять общий смысл: Playwright выполняет действия в браузере не мгновенно, поэтому тест должен дожидаться результата. Подробно асинхронный код будет разобран в отдельном модуле.

## 9. npm-скрипты

В `package.json` можно добавить скрипты для частых команд:

```json
{
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  }
}
```

После этого команды будут выглядеть так:

```bash
npm run test
npm run test:headed
npm run test:ui
npm run report
```

Скрипты полезны, потому что:

- делают команды короче;
- стандартизируют запуск тестов для всей команды;
- уменьшают риск опечаток;
- помогают новичкам быстрее понять, как работать с проектом;
- используются в CI-пайплайнах.

## Практика на занятии

### Часть 1. Демонстрация

На занятии преподаватель может показать, как создаётся новый проект Playwright через:

```bash
npm init playwright@latest
```

Во время демонстрации обратите внимание:

- какие вопросы задаёт установщик;
- почему выбирается TypeScript;
- какие файлы появляются после установки;
- где находится `playwright.config.ts`;
- где лежат тесты;
- какие команды предлагает Playwright.

### Часть 2. Практический сценарий курса

Практическую часть студент выполняет в подготовленном репозитории домашних заданий.

1. Открыть страницу репозитория домашних заданий на GitHub.
2. Склонировать репозиторий на компьютер.
3. Открыть папку репозитория в VS Code.
4. Открыть встроенный терминал VS Code.
5. Проверить версии Node.js, npm и Git.
6. Получить список веток через `git fetch origin --prune`.
7. Перейти в свою ветку модуля 01.
8. Проверить, что видны `package.json`, `playwright.config.ts`, `tests/` и `homework/`.
9. Установить зависимости.
10. Установить браузеры Playwright.
11. Запустить `npm run typecheck`.
12. Запустить `npm run test:smoke`.
13. Запустить `npm run test`.
14. Открыть HTML report.
15. Заполнить `homework/module-01-environment/result.md`.

Основной результат модуля 01 — не набор сгенерированных конфигурационных файлов, а проверенное окружение и заполненный `result.md` с результатами команд.

## Вопросы для проверки понимания

- Что такое Node.js?
- Почему Playwright нужен Node.js?
- Что такое npm?
- Что такое менеджер пакетов?
- Что такое `package.json`?
- Для чего нужен `package-lock.json`?
- Почему `node_modules` не нужно добавлять в commit в Git?
- Чем `npm install` отличается от `npm ci` на базовом уровне?
- Что делает `npm run`?
- Что такое `npx`?
- Как запустить тесты Playwright из терминала?
- Для чего нужен `playwright.config.ts`?
- Что такое assertion?
- Почему команды Playwright часто используют `await`?
- Чем полезен HTML report для Automation QA?

## Краткий итог

В этом модуле вы разобрали, из каких инструментов состоит рабочее окружение Automation QA, как обычно создаётся проект Playwright, как устроены основные файлы проекта, как работать с подготовленным репозиторием домашних заданий, запускать проверки и открывать HTML report.

Следующий важный шаг — продолжить практику: чаще запускать команды из терминала, читать вывод и постепенно привыкать к структуре проекта автоматизации.
